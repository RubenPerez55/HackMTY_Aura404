export default function DocumentPreview({ data }) { const safeUrl = /^https?:\/\//i.test(data.url ?? "") ? data.url : null; return <section className="flex items-center gap-3 rounded-2xl border border-gray-200 bg-white p-4"><span className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl bg-red-50 text-[#EB0029]"><i className="fa-solid fa-file-lines text-xl" /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-gray-800">{data.title}</p><p className="text-[10px] text-gray-400">{[data.documentType, data.date, data.size].filter(Boolean).join(" · ")}</p>{data.description && <p className="mt-1 line-clamp-2 text-xs text-gray-500">{data.description}</p>}</div>{safeUrl && <a href={safeUrl} target="_blank" rel="noreferrer" className="rounded-xl bg-gray-900 px-3 py-2 text-xs font-semibold text-white">Abrir</a>}</section>; }
