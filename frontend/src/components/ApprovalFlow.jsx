export default function ApprovalFlow({ data, onAction }) { const done = data.steps.filter((s) => s.status === "complete").length; return <section className="rounded-2xl border border-gray-200 bg-white p-4"><div className="flex justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400">Flujo de aprobación</p><h3 className="text-sm font-bold text-gray-900">{data.title}</h3></div><span className="text-xs font-bold text-[#EB0029]">{done}/{data.steps.length}</span></div><div className="mt-4 flex items-start">{data.steps.map((step, index) => <div key={step.id} className="flex flex-1 items-start"><div className="flex min-w-0 flex-1 flex-col items-center text-center"><span className={`flex h-7 w-7 items-center justify-center rounded-full text-[10px] ${step.status === "complete" ? "bg-green-600 text-white" : step.status === "current" ? "bg-[#EB0029] text-white" : "bg-gray-100 text-gray-400"}`}><i className={`fa-solid ${step.status === "complete" ? "fa-check" : "fa-circle"}`} /></span><p className="mt-1 text-[9px] font-semibold text-gray-600">{step.label}</p></div>{index < data.steps.length - 1 && <span className={`mt-3 h-0.5 w-4 ${step.status === "complete" ? "bg-green-500" : "bg-gray-200"}`} />}</div>)}</div>{data.actionLabel && <button onClick={() => onAction?.({ action: data.actionId ?? "continue_approval", actionSummary: data.actionSummary ?? data.actionLabel })} className="mt-4 w-full rounded-xl bg-[#EB0029] py-2.5 text-sm font-semibold text-white">{data.actionLabel}</button>}</section>; }
