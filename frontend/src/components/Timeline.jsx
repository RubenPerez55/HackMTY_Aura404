const ICONS = { complete: "fa-check", current: "fa-circle", pending: "fa-clock", error: "fa-xmark" };
export default function Timeline({ data }) { return <section className="rounded-2xl bg-gray-50 p-4"><h3 className="mb-4 text-sm font-bold text-gray-900">{data.title}</h3><ol>{data.events.map((event, index) => <li key={event.id} className="relative flex gap-3 pb-4 last:pb-0">{index < data.events.length - 1 && <span className="absolute left-3 top-6 h-full w-px bg-gray-200" />}<span className={`z-10 flex h-6 w-6 shrink-0 items-center justify-center rounded-full text-[9px] ${event.status === "complete" ? "bg-green-600 text-white" : event.status === "error" ? "bg-[#EB0029] text-white" : event.status === "current" ? "bg-gray-900 text-white" : "bg-gray-200 text-gray-500"}`}><i className={`fa-solid ${ICONS[event.status] ?? ICONS.pending}`} /></span><div><p className="text-xs font-semibold text-gray-800">{event.title}</p>{event.description && <p className="text-[10px] text-gray-500">{event.description}</p>}{event.date && <p className="mt-1 text-[9px] text-gray-400">{event.date}</p>}</div></li>)}</ol></section>; }
