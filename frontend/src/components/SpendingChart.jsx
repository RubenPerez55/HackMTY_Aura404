const COLORS = ["bg-[#EB0029]", "bg-orange-400", "bg-gray-800", "bg-gray-400", "bg-red-200"];
export default function SpendingChart({ data }) {
  const total = data.categories.reduce((sum, item) => sum + item.amount, 0) || 1;
  return <section className="rounded-2xl bg-gray-50 p-4"><div className="flex items-end justify-between"><div><p className="text-[10px] font-bold uppercase tracking-wider text-gray-400">{data.title}</p><p className="text-xl font-black text-gray-900">${total.toLocaleString()} <span className="text-[10px] font-normal text-gray-400">{data.currency ?? "MXN"}</span></p></div><i className="fa-solid fa-chart-pie text-[#EB0029]" /></div><div className="mt-4 flex h-3 overflow-hidden rounded-full">{data.categories.map((item, index) => <span key={item.label} className={COLORS[index % COLORS.length]} style={{ width: `${item.amount / total * 100}%` }} />)}</div><div className="mt-3 grid grid-cols-2 gap-2">{data.categories.map((item, index) => <div key={item.label} className="flex items-center gap-2 text-xs"><span className={`h-2 w-2 rounded-full ${COLORS[index % COLORS.length]}`} /><span className="truncate text-gray-500">{item.label}</span><span className="ml-auto font-semibold">{Math.round(item.amount / total * 100)}%</span></div>)}</div></section>;
}
