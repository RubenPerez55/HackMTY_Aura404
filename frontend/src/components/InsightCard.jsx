export default function InsightCard({ data, onAction }) {
  return <section className="rounded-2xl border border-gray-200 bg-white p-4"><div className="flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-red-50 text-[#EB0029]"><i className={`fa-solid ${data.iconName ?? "fa-lightbulb"}`} /></span><div className="min-w-0"><h3 className="text-sm font-bold text-gray-900">{data.title}</h3><p className="mt-1 text-sm leading-relaxed text-gray-600">{data.description}</p>{data.details?.length > 0 && <ul className="mt-3 space-y-1 text-xs text-gray-500">{data.details.map((detail, i) => <li key={i} className="flex gap-2"><span className="text-[#EB0029]">•</span><span>{detail}</span></li>)}</ul>}</div></div>{data.actionLabel && <button onClick={() => onAction?.({ action: "insight_action", actionSummary: data.actionLabel })} className="mt-4 w-full rounded-xl bg-gray-900 py-2.5 text-sm font-semibold text-white">{data.actionLabel}</button>}</section>;
}
