export default function ErrorState({ data, onAction }) { return <section className="rounded-2xl border border-red-100 bg-red-50 p-5 text-center"><span className="mx-auto flex h-11 w-11 items-center justify-center rounded-full bg-white text-[#EB0029]"><i className="fa-solid fa-triangle-exclamation" /></span><h3 className="mt-3 text-sm font-bold text-gray-900">{data.title}</h3><p className="mt-1 text-xs text-gray-600">{data.description}</p>{data.code && <p className="mt-2 text-[10px] text-gray-400">Código: {data.code}</p>}{data.retryLabel && <button onClick={() => onAction?.({ action: data.actionId ?? "retry", actionSummary: data.retryLabel })} className="mt-4 rounded-xl bg-gray-900 px-4 py-2 text-xs font-semibold text-white">{data.retryLabel}</button>}</section>; }
