export default function EmptyState({ data, onAction }) { return <section className="rounded-2xl bg-gray-50 p-6 text-center"><span className="mx-auto flex h-12 w-12 items-center justify-center rounded-full bg-white text-gray-400 shadow-sm"><i className={`fa-solid ${data.iconName ?? "fa-inbox"}`} /></span><h3 className="mt-3 text-sm font-bold text-gray-900">{data.title}</h3><p className="mt-1 text-xs text-gray-500">{data.description}</p>{data.actionLabel && <button onClick={() => onAction?.({ action: data.actionId ?? "empty_state_action", actionSummary: data.actionLabel })} className="mt-4 rounded-xl bg-[#EB0029] px-4 py-2 text-xs font-semibold text-white">{data.actionLabel}</button>}</section>; }
